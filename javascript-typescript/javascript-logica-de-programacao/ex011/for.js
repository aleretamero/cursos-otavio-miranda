// No for prcisa criar uma variável,
// criar uma condição para parar o laço,
// precisa incrementar ou decrementar a variável

//  i = index; break; incrementar
for (let i = 0; i <= 5; i++) {
  console.log(`Linha ${i}`);
}

const frutas = ["Maça", "Pêra", "Uva"];

for (let i = 0; i < frutas.length; i++) {
  console.log(frutas[i]);
}
